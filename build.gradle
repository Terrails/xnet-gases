buildscript {
    repositories {
        maven { url = 'https://files.minecraftforge.net/maven' }
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '3.+', changing: true
    }
}
apply plugin: 'net.minecraftforge.gradle'

def minecraft_version = "1.16.4"
def forge_version = "35.0.18"
def mappings_version = ["snapshot", "20201028-1.16.3"]

version = "2.0.0"
group = 'terrails.xnetgases'
archivesBaseName = "XNetGases-${minecraft_version}"

sourceCompatibility = targetCompatibility = compileJava.sourceCompatibility = compileJava.targetCompatibility = '1.8'

minecraft {
    mappings channel: mappings_version[0], version: mappings_version[1]

    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.console.level', 'debug'

            mods {
                xnetmekanism {
                    source sourceSets.main
                }
            }
        }

        server {
            workingDirectory project.file('run')
            property 'forge.logging.console.level', 'debug'

            mods {
                xnetmekanism {
                    source sourceSets.main
                }
            }
        }
    }
}

repositories {
    maven {
        url = "https://www.cursemaven.com"
    }
}

dependencies {
    minecraft "net.minecraftforge:forge:${minecraft_version}-${forge_version}"

    // XNet
    compile fg.deobf("curse.maven:xnet:3110245") // 1.16-3.0.9
    compile fg.deobf("curse.maven:rftools-base:3116387") // 1.16-2.0.7
    compile fg.deobf("curse.maven:mcjtylib:3116381") // 1.16-5.0.14

    // Mekanism
    compile fg.deobf("curse.maven:mekanism:3100510") // 1.16.4-10.0.17.444
}

jar {
    manifest {
        attributes([
            "Specification-Title": "xnetgases",
            "Specification-Version": "1", // We are version 1 of ourselves
            "Implementation-Title": project.name,
            "Implementation-Version": "${version}",
            "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}